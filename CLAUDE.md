# EstagioFrontEnd

Frontend (React + TypeScript + Vite + Tailwind) do projeto de estágio.

## Repositório do backend

O backend fica em um repositório separado, como pasta irmã deste:

`C:\Users\Pedro\OneDrive\Documents\GitHub\EstagioBackEnd`

- Stack: Java/Spring Boot (Maven — `pom.xml`, `mvnw`).
- Repo remoto: https://github.com/VitorsosterF/EstagioBackEnd
- Sempre que uma tarefa envolver endpoints, DTOs, regras de negócio, autenticação ou qualquer coisa que dependa do que a API espera/retorna, leia o código correspondente em `../EstagioBackEnd` antes de assumir o contrato da API — não adivinhar a partir do front.
